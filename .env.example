# Copy to `.env` / `.env.local` and adjust values.

# PostgreSQL connection (required)
DATABASE_URL="postgresql://marginlane_user:YOUR_PASSWORD@localhost:5432/marginlane_dev"

# Optional: set to "production" on the host (Next.js)
# NODE_ENV=production
